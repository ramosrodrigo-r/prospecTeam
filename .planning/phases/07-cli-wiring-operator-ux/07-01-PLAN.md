---
phase: 07-cli-wiring-operator-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/args.js
  - src/utils/filter.js
  - src/stages/dedup.js
  - src/stages/fetch.js
  - tests/unit/args.test.js
  - tests/unit/filter.test.js
  - tests/unit/dedup.test.js
  - package.json
autonomous: true
requirements:
  - OPS-01

must_haves:
  truths:
    - "parseArgs() uses Commander.js internally and returns {city, category}"
    - "Missing --city or --category produces Commander native error with usage example"
    - "filterBusinesses calls onSkip callback for every discarded prospect"
    - "dedupProspects calls onSkip callback for every fully-contacted prospect"
  artifacts:
    - path: "src/utils/args.js"
      provides: "Commander.js-based CLI arg parsing"
      contains: "commander"
    - path: "src/utils/filter.js"
      provides: "filterBusinesses with onSkip callback"
      contains: "onSkip"
    - path: "src/stages/dedup.js"
      provides: "dedupProspects with onSkip callback"
      contains: "onSkip"
    - path: "package.json"
      provides: "commander dependency"
      contains: "commander"
  key_links:
    - from: "src/utils/args.js"
      to: "commander"
      via: "import { Command } from 'commander'"
      pattern: "Command.*commander"
    - from: "src/stages/fetch.js"
      to: "src/utils/filter.js"
      via: "filterBusinesses(results, onSkip)"
      pattern: "filterBusinesses.*onSkip"
---

<objective>
Migrate CLI arg parsing to Commander.js and add onSkip callbacks to filter and dedup stages.

Purpose: Enables Plan 02 to wire skip logging and Commander into bin/prospect.js. These are the foundational signature changes that the pipeline orchestrator will consume.
Output: Updated args.js (Commander), filter.js (onSkip), dedup.js (onSkip), fetch.js (threads onSkip), updated tests, commander in package.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-wiring-operator-ux/07-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commander.js args + onSkip callbacks in filter and dedup</name>
  <files>package.json, src/utils/args.js, src/utils/filter.js, src/stages/dedup.js, src/stages/fetch.js, tests/unit/args.test.js, tests/unit/filter.test.js, tests/unit/dedup.test.js</files>
  <read_first>
    - src/utils/args.js (current parseArgs implementation to replace)
    - src/utils/filter.js (current filterBusinesses signature)
    - src/stages/dedup.js (current dedupProspects signature)
    - src/stages/fetch.js (calls filterBusinesses — must thread onSkip)
    - tests/unit/args.test.js (existing test patterns)
    - tests/unit/filter.test.js (existing test patterns)
    - tests/unit/dedup.test.js (existing test patterns)
    - package.json (current dependencies)
  </read_first>
  <behavior>
    - Test: parseArgs(['node', 'script', '--city', 'Campinas', '--category', 'academia']) returns { city: 'Campinas', category: 'academia' }
    - Test: parseArgs(['node', 'script']) throws or exits (Commander native error for missing required options)
    - Test: filterBusinesses([{website: 'https://real.com', name: 'Biz'}], onSkip) calls onSkip with (prospect, 'has-website', 'https://real.com')
    - Test: filterBusinesses([{website: null, name: 'Biz'}], onSkip) does NOT call onSkip
    - Test: filterBusinesses without onSkip still works (backward compatible)
    - Test: dedupProspects where both channels done calls onSkip with (prospect, 'already-contacted', ['wa', 'email'])
    - Test: dedupProspects where one channel pending does NOT call onSkip
    - Test: dedupProspects without onSkip still works (backward compatible)
  </behavior>
  <action>
    **Step 1: Install Commander.js**
    Run `npm install commander` to add it to package.json dependencies.

    **Step 2: Rewrite src/utils/args.js (per D-01, D-02, D-03, D-04)**
    Replace entire file. The new implementation:
    ```
    import { Command } from 'commander'

    export function parseArgs(argv) {
      const program = new Command()
      program
        .name('prospect')
        .description('ProspecTeam — bot de outreach via WhatsApp e email')
        .requiredOption('--city <city>', 'cidade para buscar negócios')
        .requiredOption('--category <category>', 'categoria de negócio')
        .addHelpText('after', '\nExemplo:\n  node bin/prospect.js --city "Campinas" --category "academia"')
        .exitOverride()

      program.parse(argv)
      const opts = program.opts()
      return { city: opts.city, category: opts.category }
    }
    ```
    Key details:
    - `.requiredOption` for both --city and --category (per D-03, Commander handles missing arg error natively)
    - `.exitOverride()` so tests can catch the thrown CommanderError instead of process.exit
    - `.addHelpText('after', ...)` adds the example command (per D-04)
    - Export signature unchanged: `parseArgs(argv)` returns `{city, category}`

    **Step 3: Update tests/unit/args.test.js**
    Replace entire file. New tests:
    - Test parsing valid args returns `{city, category}` — use `parseArgs(['node', 'script', '--city', 'Campinas', '--category', 'academia'])`
    - Test missing --city throws CommanderError — wrap in assert.throws, check error.code === 'commander.missingMandatoryOptionValue' or 'commander.missingRequiredOption' (Commander internal code)
    - Test missing --category throws CommanderError similarly
    - Test --help includes 'ProspecTeam' and 'Campinas' — parse with ['node', 'script', '--help'], catch the thrown error (exitOverride makes --help throw too), check program output or just verify no crash
    - Remove old test for "treats next flag as value" — Commander handles this differently

    **Step 4: Add onSkip to src/utils/filter.js (per D-05, D-06)**
    Change `filterBusinesses` signature to `filterBusinesses(prospects, onSkip)`.
    In the filter callback, when a prospect IS discarded (hasRealWebsite returns true), call:
    ```
    if (onSkip) onSkip(prospect, 'has-website', prospect.website)
    ```
    Implementation:
    ```
    export function filterBusinesses(prospects, onSkip) {
      return prospects.filter(prospect => {
        if (hasRealWebsite(prospect.website)) {
          if (onSkip) onSkip(prospect, 'has-website', prospect.website)
          return false
        }
        return true
      })
    }
    ```

    **Step 5: Update tests/unit/filter.test.js**
    Add new tests (keep all existing tests — they still pass since onSkip is optional):
    - `it('calls onSkip with prospect, reason, and URL when business has real website')` — pass a mock onSkip function, assert it was called with correct args
    - `it('does not call onSkip when business has no real website')` — pass mock, assert callCount === 0
    - `it('works without onSkip callback (backward compatible)')` — existing tests already cover this

    **Step 6: Add onSkip to src/stages/dedup.js (per D-08, D-09)**
    Change signature to `dedupProspects(prospects, onSkip)`.
    When both channels are done (waDone && emailDone), call:
    ```
    if (onSkip) {
      const channels = []
      if (waDone) channels.push('wa')
      if (emailDone) channels.push('email')
      onSkip(p, 'already-contacted', channels)
    }
    ```
    Full implementation:
    ```
    import { isDuplicate } from '../history.js'

    export function dedupProspects(prospects, onSkip) {
      return prospects.filter(p => {
        const waDone = isDuplicate(p.placeId, 'wa')
        const emailDone = isDuplicate(p.placeId, 'email')
        if (waDone && emailDone) {
          if (onSkip) onSkip(p, 'already-contacted', ['wa', 'email'])
          return false
        }
        return true
      })
    }
    ```

    **Step 7: Update tests/unit/dedup.test.js**
    Add tests for onSkip callback:
    - `it('calls onSkip when both channels already sent')` — mock isDuplicate to return true for both, verify onSkip called with (prospect, 'already-contacted', ['wa', 'email'])
    - `it('does not call onSkip when at least one channel pending')` — mock isDuplicate to return true for wa only, verify onSkip NOT called

    **Step 8: Thread onSkip through src/stages/fetch.js**
    Change `fetchProspects` to accept an options object with onSkip:
    ```
    export async function fetchProspects({ city, category, apiKey, onSkip }) {
      ...
      return filterBusinesses(results, onSkip)
    }
    ```
    The `onSkip` parameter is optional — existing callers without it still work since filterBusinesses handles undefined onSkip.
  </action>
  <verify>
    <automated>cd /home/rodrigo/prospecTeam && node --test tests/unit/args.test.js tests/unit/filter.test.js tests/unit/dedup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "commander" in dependencies
    - src/utils/args.js contains `import { Command } from 'commander'`
    - src/utils/args.js contains `.requiredOption('--city`
    - src/utils/args.js contains `.requiredOption('--category`
    - src/utils/args.js contains `ProspecTeam`
    - src/utils/args.js contains `Campinas`
    - src/utils/args.js contains `export function parseArgs`
    - src/utils/filter.js contains `function filterBusinesses(prospects, onSkip)`
    - src/utils/filter.js contains `onSkip(prospect, 'has-website', prospect.website)`
    - src/stages/dedup.js contains `function dedupProspects(prospects, onSkip)`
    - src/stages/dedup.js contains `onSkip(p, 'already-contacted'`
    - src/stages/fetch.js contains `onSkip` parameter
    - tests/unit/args.test.js contains `commander` or `CommanderError` or `requiredOption`
    - tests/unit/filter.test.js contains `onSkip`
    - tests/unit/dedup.test.js contains `onSkip`
    - `node --test tests/unit/args.test.js tests/unit/filter.test.js tests/unit/dedup.test.js` exits 0
  </acceptance_criteria>
  <done>Commander.js replaces custom parseArgs with .requiredOption for --city and --category. filterBusinesses and dedupProspects accept optional onSkip callbacks. All tests pass. fetch.js threads onSkip to filterBusinesses.</done>
</task>

</tasks>

<verification>
- All 3 updated test files pass: `node --test tests/unit/args.test.js tests/unit/filter.test.js tests/unit/dedup.test.js`
- Full test suite still passes: `node --test tests/unit/*.test.js`
- `grep -c 'commander' package.json` returns at least 1
</verification>

<success_criteria>
1. Commander.js is installed and args.js uses it with .requiredOption
2. filterBusinesses(prospects, onSkip) calls onSkip for discarded prospects
3. dedupProspects(prospects, onSkip) calls onSkip for fully-contacted prospects
4. fetchProspects threads onSkip to filterBusinesses
5. All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-wiring-operator-ux/07-01-SUMMARY.md`
</output>
