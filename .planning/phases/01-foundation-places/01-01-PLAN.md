---
phase: 01-foundation-places
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - .env.example
  - README.md
  - tests/unit/args.test.js
  - tests/unit/env.test.js
  - tests/unit/fetch.test.js
  - tests/unit/places.test.js
autonomous: true
requirements:
  - SRCH-01
  - SRCH-03
must_haves:
  truths:
    - "Project is a valid ESM Node.js package with dotenv installed"
    - "All four unit test files exist and run (all tests fail RED — no implementation yet)"
    - ".env.example documents required credentials"
  artifacts:
    - path: "package.json"
      provides: "ESM project configuration"
      contains: '"type": "module"'
    - path: ".gitignore"
      provides: "Credential protection"
      contains: ".env"
    - path: ".env.example"
      provides: "Credential template"
      contains: "GOOGLE_PLACES_API_KEY"
    - path: "tests/unit/args.test.js"
      provides: "CLI arg parsing tests"
      min_lines: 20
    - path: "tests/unit/env.test.js"
      provides: "Billing guard tests"
      min_lines: 10
    - path: "tests/unit/fetch.test.js"
      provides: "Pagination and field mapping tests"
      min_lines: 40
    - path: "tests/unit/places.test.js"
      provides: "FieldMask and debug logging tests"
      min_lines: 20
  key_links:
    - from: "tests/unit/fetch.test.js"
      to: "src/stages/fetch.js"
      via: "import { fetchProspects }"
      pattern: "import.*fetchProspects.*from.*stages/fetch"
    - from: "tests/unit/places.test.js"
      to: "src/services/places.js"
      via: "import { searchPlaces }"
      pattern: "import.*searchPlaces.*from.*services/places"
---

<objective>
Scaffold the ESM Node.js project and create all Wave 0 test stubs with failing tests (RED phase).

Purpose: Establish the project foundation (package.json, dotenv, .gitignore, .env.example) and create comprehensive unit tests for all Phase 1 behaviors BEFORE any implementation code exists. Tests define the contracts that Plan 02 implements against.

Output: A runnable project with `npm install` completing successfully, and 4 test files that all fail (RED) because the source modules they import do not exist yet.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-places/1-CONTEXT.md
@.planning/phases/01-foundation-places/01-RESEARCH.md
@.planning/phases/01-foundation-places/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ESM project with dotenv</name>
  <read_first>
    - .planning/phases/01-foundation-places/01-RESEARCH.md (Pattern 1: ESM Package Setup, Pattern 2: dotenv in ESM)
  </read_first>
  <files>package.json, .gitignore, .env.example, README.md</files>
  <action>
Create the project scaffold from scratch:

1. Create `package.json` with these exact contents:
```json
{
  "name": "prospecteam",
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=21" },
  "scripts": {
    "start": "node bin/prospect.js",
    "test": "node --test tests/unit/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
```
Key fields: `"type": "module"` enables ESM across all files (per D-02). `"engines": ">=21"` guarantees stable native fetch (per D-02). `"scripts.test"` uses `node:test` built-in (zero extra dependency).

2. Create `.gitignore`:
```
.env
node_modules/
```

3. Create `.env.example`:
```
# Google Places API key — required for all searches
# Get yours at: https://console.cloud.google.com/apis/credentials
# IMPORTANT: Before running, configure in Google Cloud Console:
#   1. Enable "Places API (New)" in APIs & Services
#   2. Set a $10 billing alert in Billing > Budgets & Alerts
#   3. Set a daily quota cap in APIs & Services > Places API (New) > Quotas
GOOGLE_PLACES_API_KEY=
```

4. Create `README.md` with minimal content:
```markdown
# ProspecTeam

Automated business prospecting via Google Places API.

## Prerequisites

- Node.js >= 21
- Google Cloud project with Places API (New) enabled
- **IMPORTANT: Before any live API call, configure billing guards:**
  1. Set a **$10 billing alert** in Google Cloud Console > Billing > Budgets & Alerts
  2. Set a **daily quota cap** in APIs & Services > Places API (New) > Quotas
  - Requesting `nationalPhoneNumber`, `websiteUri`, and `rating` triggers the Enterprise SKU (highest billing tier)

## Setup

```bash
cp .env.example .env
# Edit .env with your GOOGLE_PLACES_API_KEY
npm install
```

## Usage

```bash
node bin/prospect.js --city "Sao Paulo" --category "restaurante"

# Debug mode (logs request headers and body to stderr)
DEBUG=1 node bin/prospect.js --city "Sao Paulo" --category "restaurante"
```
```

5. Create empty directories: `mkdir -p bin src/services src/stages tests/unit`

6. Run `npm install` to install dotenv and generate `package-lock.json`.
  </action>
  <verify>
    <automated>cd /home/rodrigo/prospecTeam && node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); assert(p.type==='module'); assert(p.engines.node==='>=21'); assert(p.dependencies.dotenv); console.log('OK')" && test -f .gitignore && test -f .env.example && test -d node_modules/dotenv && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"type": "module"`
    - `package.json` contains `"engines": { "node": ">=21" }`
    - `package.json` contains `"dotenv": "^17.3.1"` in dependencies
    - `package.json` contains `"test": "node --test tests/unit/*.test.js"` in scripts
    - `.gitignore` contains `.env` and `node_modules/`
    - `.env.example` contains `GOOGLE_PLACES_API_KEY=`
    - `.env.example` contains text about $10 billing alert
    - `README.md` contains billing guard instructions mentioning Enterprise SKU
    - `node_modules/dotenv/` directory exists after `npm install`
    - Directories exist: `bin/`, `src/services/`, `src/stages/`, `tests/unit/`
  </acceptance_criteria>
  <done>ESM project scaffolded with dotenv installed, .gitignore blocks .env, .env.example documents billing guard prerequisites, README includes billing guard warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 test stubs (RED phase — all tests must fail)</name>
  <read_first>
    - /home/rodrigo/prospecTeam/package.json (verify ESM and test script exist from Task 1)
    - .planning/phases/01-foundation-places/01-RESEARCH.md (Pattern 3: Places API single page, Pattern 4: Pagination loop, Pattern 5: CLI arg parsing)
    - .planning/phases/01-foundation-places/01-VALIDATION.md (Per-Task Verification Map and Wave 0 Requirements)
  </read_first>
  <files>tests/unit/args.test.js, tests/unit/env.test.js, tests/unit/fetch.test.js, tests/unit/places.test.js</files>
  <behavior>
    SRCH-01 arg parsing (args.test.js):
    - parseArgs(['node','script','--city','Sao Paulo','--category','restaurante']) returns { city: 'Sao Paulo', category: 'restaurante' }
    - parseArgs(['node','script','--city','Sao Paulo']) returns object WITHOUT category key (or undefined)
    - parseArgs(['node','script']) returns object with neither city nor category

    D-07 billing guard (env.test.js):
    - validateEnv() with GOOGLE_PLACES_API_KEY set returns the key string
    - validateEnv() with GOOGLE_PLACES_API_KEY unset throws or returns null/falsy

    SRCH-01 pagination + SRCH-03 field mapping (fetch.test.js):
    - fetchProspects with single-page response (no nextPageToken) returns array of mapped prospects
    - fetchProspects with two-page response (first has nextPageToken, second does not) returns combined array
    - Each prospect has shape { placeId, name, rating, phone, website, email: null }
    - displayName.text is extracted (not the object itself)
    - Missing fields default to null

    D-03 FieldMask + D-13 debug (places.test.js):
    - searchPlaces sends X-Goog-FieldMask header containing 'places.id,places.displayName,places.websiteUri,places.nationalPhoneNumber,places.rating,nextPageToken'
    - searchPlaces sends X-Goog-Api-Key header with the provided apiKey
    - searchPlaces sends POST to 'https://places.googleapis.com/v1/places:searchText'
    - When DEBUG=1, console.error is called with request details
  </behavior>
  <action>
Create all four test files using `node:test` and `node:assert`. Tests import from the source modules that DO NOT exist yet -- this is intentional (RED phase). The test runner will report failures because the imports fail.

**CRITICAL implementation details for the test mocking approach:**

Since `fetch` is a global and `node:test` has `mock` built-in, tests should use `t.mock.method(globalThis, 'fetch', mockFn)` to intercept HTTP calls. For `console.error` debug tests, use `t.mock.method(console, 'error')`.

Each test file must use `import { describe, it, mock } from 'node:test'` and `import assert from 'node:assert/strict'`.

**File: `tests/unit/args.test.js`**
- Import `{ parseArgs }` from `../../src/utils/args.js`
- Test: `parseArgs(['node','script','--city','Sao Paulo','--category','restaurante'])` deep-equals `{ city: 'Sao Paulo', category: 'restaurante' }`
- Test: `parseArgs(['node','script','--city','Sao Paulo'])` — `result.category` is `undefined`
- Test: `parseArgs(['node','script'])` — both `result.city` and `result.category` are `undefined`
- Test: `parseArgs(['node','script','--city','--category','restaurante'])` — `result.city` is `'--category'` or similar edge case handling

**File: `tests/unit/env.test.js`**
- Import `{ validateEnv }` from `../../src/utils/env.js`
- Test: With `process.env.GOOGLE_PLACES_API_KEY = 'test-key-123'`, `validateEnv()` returns `'test-key-123'`
- Test: With `delete process.env.GOOGLE_PLACES_API_KEY`, `validateEnv()` throws an error with message containing `'GOOGLE_PLACES_API_KEY'`
- Test: With `process.env.GOOGLE_PLACES_API_KEY = ''`, `validateEnv()` throws (empty string treated as missing)
- Cleanup: restore original env value in afterEach

**File: `tests/unit/fetch.test.js`**
- Import `{ fetchProspects }` from `../../src/stages/fetch.js`
- Mock `globalThis.fetch` to return controlled responses
- Single-page test: mock fetch returns `{ places: [{ id: 'place1', displayName: { text: 'Padaria', languageCode: 'pt' }, rating: 4.5, nationalPhoneNumber: '(11) 98765-4321', websiteUri: 'https://padaria.com' }] }` (no nextPageToken). Assert result is `[{ placeId: 'place1', name: 'Padaria', rating: 4.5, phone: '(11) 98765-4321', website: 'https://padaria.com', email: null }]`
- Two-page test: first fetch returns response with `nextPageToken: 'TOKEN_ABC'` and one place, second fetch returns response with different place and no nextPageToken. Assert combined array has 2 items. Assert fetch was called twice. Assert second call body contains `"pageToken":"TOKEN_ABC"`.
- Field mapping test: place with missing `rating`, `nationalPhoneNumber`, `websiteUri` all map to `null`. `email` is always `null`.
- displayName test: place with `displayName: { text: 'Test Name', languageCode: 'en' }` maps to `name: 'Test Name'` (not the object)
- Mock fetch setup: `globalThis.fetch = async (url, opts) => ({ ok: true, json: async () => mockResponse })` — restore original fetch in afterEach

**File: `tests/unit/places.test.js`**
- Import `{ searchPlaces }` from `../../src/services/places.js`
- Mock `globalThis.fetch` to capture the arguments passed
- FieldMask test: Call `searchPlaces({ query: 'test', apiKey: 'KEY123' })`. Assert the `fetch` call's headers object contains `'X-Goog-FieldMask': 'places.id,places.displayName,places.websiteUri,places.nationalPhoneNumber,places.rating,nextPageToken'` (exact string match)
- API key test: Assert headers contain `'X-Goog-Api-Key': 'KEY123'`
- URL test: Assert fetch was called with URL `'https://places.googleapis.com/v1/places:searchText'`
- Method test: Assert fetch options include `method: 'POST'`
- Body test: Assert parsed body contains `textQuery: 'test'` and `pageSize: 20`
- pageToken test: Call with `pageToken: 'TOKEN_XYZ'`. Assert body contains `pageToken: 'TOKEN_XYZ'`
- No pageToken test: Call without pageToken. Assert body does NOT contain `pageToken` key
- Debug test: Set `process.env.DEBUG = '1'`, call searchPlaces, assert `console.error` was called. Cleanup: delete process.env.DEBUG
- No debug test: Ensure DEBUG is unset, call searchPlaces, assert `console.error` was NOT called

All tests will FAIL (RED) because the source files they import do not exist yet. This is correct and expected. Run `npm test` to confirm all 4 files produce import errors.
  </action>
  <verify>
    <automated>cd /home/rodrigo/prospecTeam && node --test tests/unit/*.test.js 2>&1; echo "EXIT CODE: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/args.test.js` exists and contains `import { parseArgs } from '../../src/utils/args.js'`
    - `tests/unit/args.test.js` contains at least 3 test cases (describe/it blocks)
    - `tests/unit/env.test.js` exists and contains `import { validateEnv } from '../../src/utils/env.js'`
    - `tests/unit/env.test.js` contains `GOOGLE_PLACES_API_KEY` string in test logic
    - `tests/unit/fetch.test.js` exists and contains `import { fetchProspects } from '../../src/stages/fetch.js'`
    - `tests/unit/fetch.test.js` contains `displayName` in test assertions (verifying .text extraction)
    - `tests/unit/fetch.test.js` contains `nextPageToken` in test data (verifying pagination)
    - `tests/unit/fetch.test.js` contains `email: null` in expected output
    - `tests/unit/places.test.js` exists and contains `import { searchPlaces } from '../../src/services/places.js'`
    - `tests/unit/places.test.js` contains the exact FieldMask string `places.id,places.displayName,places.websiteUri,places.nationalPhoneNumber,places.rating,nextPageToken`
    - `tests/unit/places.test.js` contains `DEBUG` in test logic
    - Running `npm test` exits with non-zero code (RED — source files do not exist yet)
    - All 4 test files are valid JavaScript (no syntax errors if imports were available)
  </acceptance_criteria>
  <done>Four test files exist covering all Phase 1 behaviors (arg parsing, env validation, pagination, field mapping, FieldMask, debug logging). All tests fail RED because source modules are not yet implemented. Wave 0 complete.</done>
</task>

</tasks>

<verification>
- `npm test` runs all 4 test files (they will all fail RED at this stage — expected)
- `package.json` is valid JSON with `type: "module"` and `engines.node: ">=21"`
- `node_modules/dotenv` directory exists
- `.env.example` contains `GOOGLE_PLACES_API_KEY=`
- All test files contain correct imports from their target source modules
</verification>

<success_criteria>
1. `npm install` completes without errors
2. All 4 test files exist and import from the correct source paths
3. `npm test` runs all 4 files and reports failures (RED) because source modules do not exist
4. .env.example documents the billing guard prerequisites
5. .gitignore blocks .env and node_modules
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-places/01-01-SUMMARY.md`
</output>
