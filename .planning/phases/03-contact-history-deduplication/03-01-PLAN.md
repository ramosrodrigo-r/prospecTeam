---
phase: 03-contact-history-deduplication
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/history.test.js
  - src/history.js
autonomous: true
requirements:
  - HIST-01
  - HIST-03

must_haves:
  truths:
    - "loadHistory() cria data/ automaticamente na primeira execucao"
    - "loadHistory() retorna graciosamente se history.json nao existir"
    - "isDuplicate(placeId) retorna true para place ja registrado"
    - "isDuplicate(placeId) retorna false para place desconhecido"
    - "recordSend(placeId) persiste entry em data/history.json via write-then-rename atomico"
    - "Apos recordSend, isDuplicate retorna true para o mesmo placeId"
    - "recordSend nao corrompe historico existente ao adicionar nova entrada"
  artifacts:
    - path: "src/history.js"
      provides: "Modulo de historico com loadHistory, isDuplicate, recordSend"
      exports: ["loadHistory", "isDuplicate", "recordSend"]
    - path: "tests/unit/history.test.js"
      provides: "Testes unitarios para os tres exports de history.js"
      min_lines: 50
  key_links:
    - from: "src/history.js"
      to: "data/history.json"
      via: "readFileSync + writeFileSync + renameSync"
      pattern: "renameSync.*HISTORY_FILE"
    - from: "src/history.js"
      to: "data/history.json.tmp"
      via: "writeFileSync para tmp, renameSync para final"
      pattern: "writeFileSync.*HISTORY_TMP"
---

<objective>
TDD do modulo history.js — testes RED primeiro, implementacao GREEN depois.

Purpose: Criar o modulo central de historico com persistencia atomica (write-then-rename) que sera usado por todos os senders nas fases 5-6. Sem history.js, nao ha deduplicacao.
Output: `src/history.js` com 3 exports testados + `tests/unit/history.test.js` verde.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contact-history-deduplication/03-CONTEXT.md
@.planning/phases/03-contact-history-deduplication/03-RESEARCH.md

<interfaces>
<!-- Padrao de teste existente no projeto (tests/unit/filter.test.js): -->
```javascript
import { describe, it } from 'node:test'
import assert from 'node:assert/strict'
import { filterBusinesses } from '../../src/utils/filter.js'

describe('filterBusinesses', () => {
  it('includes business with null website', () => {
    const result = filterBusinesses([{ website: null }])
    assert.equal(result.length, 1)
  })
})
```

<!-- Padrao de modulo existente (src/utils/filter.js): -->
```javascript
export function filterBusinesses(prospects) {
  return prospects.filter(prospect => !hasRealWebsite(prospect.website))
}
```

<!-- Prospect shape from src/stages/fetch.js: -->
```javascript
{
  placeId: place.id,        // string — chave usada pelo history
  name: place.displayName?.text ?? null,
  rating: place.rating ?? null,
  phone: place.nationalPhoneNumber ?? null,
  website: place.websiteUri ?? null,
  email: null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Escrever testes para history.js (todos devem falhar)</name>
  <files>tests/unit/history.test.js</files>
  <read_first>
    tests/unit/filter.test.js
    src/stages/fetch.js
  </read_first>
  <behavior>
    - loadHistory() em diretorio inexistente: nao lanca erro, isDuplicate retorna false
    - loadHistory() com arquivo existente: isDuplicate retorna true para place_id presente no arquivo
    - isDuplicate('ChIJunknown') retorna false quando historico esta vazio
    - isDuplicate('ChIJ123') retorna true quando 'ChIJ123' foi carregado do arquivo
    - recordSend('ChIJnew') cria entry com sentAt ISO no arquivo data/history.json
    - recordSend('ChIJnew') faz isDuplicate('ChIJnew') retornar true imediatamente
    - recordSend nao apaga entries existentes — historico anterior permanece intacto
    - recordSend usa write-then-rename: history.json.tmp existe momentaneamente, history.json e o resultado final
  </behavior>
  <action>
Criar `tests/unit/history.test.js` com os testes abaixo. Usar `node:test` (describe/it/beforeEach/afterEach) e `node:assert/strict`.

IMPORTANTE: Os testes precisam isolar estado entre runs. Como `history.js` usa module-scope state (Map), cada teste DEVE chamar `loadHistory()` no setup para reinicializar o Map. Para evitar poluir `data/history.json` do projeto real, usar abordagem pragmatica: o modulo usa o caminho fixo `data/history.json`, entao os testes vao operar nesse caminho e limpar no afterEach.

Estrutura dos testes:

```javascript
import { describe, it, beforeEach, afterEach } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync, rmSync, mkdirSync, existsSync } from 'node:fs'
import { loadHistory, isDuplicate, recordSend } from '../../src/history.js'

const HISTORY_FILE = './data/history.json'
const HISTORY_TMP = './data/history.json.tmp'

describe('history', () => {
  beforeEach(() => {
    // Garantir diretorio existe e limpar arquivo
    mkdirSync('./data', { recursive: true })
    try { rmSync(HISTORY_FILE) } catch {}
    try { rmSync(HISTORY_TMP) } catch {}
    loadHistory()  // reinicializa Map interno
  })

  afterEach(() => {
    try { rmSync(HISTORY_FILE) } catch {}
    try { rmSync(HISTORY_TMP) } catch {}
  })
```

Testes a incluir (8 testes):

1. `'loadHistory no arquivo inexistente nao lanca erro'` — rmSync do arquivo, chamar loadHistory(), nao deve lancar
2. `'isDuplicate retorna false para placeId desconhecido'` — assert.equal(isDuplicate('ChIJunknown'), false)
3. `'loadHistory carrega entries de arquivo existente'` — writeFileSync com JSON `{"ChIJ123": {"sentAt": "2026-01-01T00:00:00.000Z"}}`, chamar loadHistory(), assert.equal(isDuplicate('ChIJ123'), true)
4. `'recordSend persiste entry em history.json'` — recordSend('ChIJnew'), ler arquivo, parsear JSON, assert que 'ChIJnew' existe como chave e tem campo sentAt string
5. `'recordSend faz isDuplicate retornar true imediatamente'` — recordSend('ChIJfresh'), assert.equal(isDuplicate('ChIJfresh'), true)
6. `'recordSend nao apaga entries existentes'` — writeFileSync com JSON contendo 'ChIJold', loadHistory(), recordSend('ChIJnew'), ler arquivo, assert que tanto 'ChIJold' quanto 'ChIJnew' existem
7. `'recordSend usa write-then-rename (history.json valido)'` — recordSend('ChIJtest'), assert que history.json existe e JSON.parse nao lanca, assert que history.json.tmp NAO existe (rename remove o tmp)
8. `'loadHistory cria diretorio data/ se nao existir'` — rmSync('./data', { recursive: true, force: true }), loadHistory(), assert(existsSync('./data'))

Rodar `node --test tests/unit/history.test.js` — TODOS devem falhar (src/history.js nao existe ainda).
  </action>
  <verify>
    <automated>node --test tests/unit/history.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/history.test.js existe e contem `import { loadHistory, isDuplicate, recordSend } from '../../src/history.js'`
    - tests/unit/history.test.js contem pelo menos 8 `it(` calls
    - tests/unit/history.test.js contem `describe('history'`
    - tests/unit/history.test.js contem `beforeEach` e `afterEach` com cleanup
    - `node --test tests/unit/history.test.js` falha (exit code != 0) porque src/history.js nao existe
  </acceptance_criteria>
  <done>8 testes RED escritos cobrindo loadHistory, isDuplicate e recordSend. Todos falham porque src/history.js ainda nao existe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar src/history.js para passar todos os testes</name>
  <files>src/history.js</files>
  <read_first>
    tests/unit/history.test.js
    src/utils/filter.js
    src/stages/fetch.js
  </read_first>
  <behavior>
    - loadHistory() cria data/ via mkdirSync recursive, carrega JSON em Map, trata ENOENT graciosamente
    - isDuplicate(placeId) retorna boolean via Map.has()
    - recordSend(placeId) atualiza Map + escreve arquivo atomicamente via writeFileSync tmp + renameSync
  </behavior>
  <action>
Criar `src/history.js` com exatamente esta implementacao (per D-01 through D-10):

```javascript
import { readFileSync, writeFileSync, renameSync, mkdirSync } from 'node:fs'
import { join, dirname } from 'node:path'
import { fileURLToPath } from 'node:url'

const __dirname = dirname(fileURLToPath(import.meta.url))
const DATA_DIR = join(__dirname, '..', 'data')
const HISTORY_FILE = join(DATA_DIR, 'history.json')
const HISTORY_TMP = HISTORY_FILE + '.tmp'

let historyMap = new Map()

export function loadHistory() {
  mkdirSync(DATA_DIR, { recursive: true })
  try {
    const raw = readFileSync(HISTORY_FILE, 'utf8')
    historyMap = new Map(Object.entries(JSON.parse(raw)))
  } catch (err) {
    if (err.code !== 'ENOENT') throw err
    historyMap = new Map()
  }
}

export function isDuplicate(placeId) {
  return historyMap.has(placeId)
}

export function recordSend(placeId) {
  historyMap.set(placeId, { sentAt: new Date().toISOString() })
  const obj = Object.fromEntries(historyMap)
  writeFileSync(HISTORY_TMP, JSON.stringify(obj, null, 2), 'utf8')
  renameSync(HISTORY_TMP, HISTORY_FILE)
}
```

Decisoes implementadas:
- D-01: formato `{ place_id: { sentAt: ISO8601 } }` via Object.fromEntries(Map)
- D-02: apenas `sentAt` salvo junto com o ID
- D-04: Map in-memory para lookup O(1)
- D-05/D-06: write-then-rename atomico via writeFileSync + renameSync
- D-08: exporta exatamente `loadHistory`, `isDuplicate`, `recordSend`
- D-09: mkdirSync({ recursive: true }) em loadHistory
- D-10: try/catch ENOENT para primeira execucao

Rodar `node --test tests/unit/history.test.js` — TODOS devem passar.
  </action>
  <verify>
    <automated>node --test tests/unit/history.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/history.js existe e contem `export function loadHistory(`
    - src/history.js contem `export function isDuplicate(`
    - src/history.js contem `export function recordSend(`
    - src/history.js contem `renameSync(HISTORY_TMP, HISTORY_FILE)`
    - src/history.js contem `mkdirSync(DATA_DIR, { recursive: true })`
    - src/history.js contem `if (err.code !== 'ENOENT') throw err`
    - src/history.js contem `Object.fromEntries(historyMap)`
    - `node --test tests/unit/history.test.js` passa (exit code 0)
    - `node --test tests/unit/*.test.js` passa (nenhum teste existente quebrado)
  </acceptance_criteria>
  <done>src/history.js implementado com 3 exports. Todos os 8 testes verdes. Suite completa sem regressoes.</done>
</task>

</tasks>

<verification>
- `node --test tests/unit/history.test.js` — 8 testes verdes
- `node --test tests/unit/*.test.js` — suite completa sem regressoes
- `grep -c "export function" src/history.js` retorna 3
- `ls data/` confirma que diretorio existe apos rodar testes
</verification>

<success_criteria>
- src/history.js exporta loadHistory, isDuplicate, recordSend
- 8 testes unitarios passam cobrindo HIST-01 (load/isDuplicate) e HIST-03 (recordSend atomico)
- Nenhum teste existente das fases 1-2 quebrado
- Arquivo history.json usa formato { placeId: { sentAt: ISO } } conforme D-01
- Write-then-rename atomico implementado conforme D-05/D-06
</success_criteria>

<output>
After completion, create `.planning/phases/03-contact-history-deduplication/03-01-SUMMARY.md`
</output>
