---
phase: 02-business-filter-phone-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/filter.test.js
  - tests/unit/phone.test.js
  - src/utils/filter.js
  - src/utils/phone.js
autonomous: true
requirements: [SRCH-02, WA-03]

must_haves:
  truths:
    - "filterBusinesses([{website: null}]) returns array with 1 element"
    - "filterBusinesses([{website: 'https://minhapadaria.com.br'}]) returns empty array"
    - "filterBusinesses([{website: 'https://www.instagram.com/biz'}]) returns array with 1 element"
    - "filterBusinesses([{website: 'linktr.ee/biz'}]) returns array with 1 element"
    - "normalizePhone('+55 (11) 98765-4321') returns '5511987654321'"
    - "normalizePhone('(11) 3456-7890') returns '551134567890'"
    - "normalizePhone('12345') returns null and logs a warning"
    - "normalizePhone(null) returns null without crashing"
  artifacts:
    - path: "tests/unit/filter.test.js"
      provides: "Unit tests for business website filter (8 cases)"
      min_lines: 50
    - path: "tests/unit/phone.test.js"
      provides: "Unit tests for Brazilian phone normalization (12+ cases)"
      min_lines: 60
    - path: "src/utils/filter.js"
      provides: "filterBusinesses pure function"
      exports: ["filterBusinesses"]
    - path: "src/utils/phone.js"
      provides: "normalizePhone pure function"
      exports: ["normalizePhone"]
  key_links:
    - from: "tests/unit/filter.test.js"
      to: "src/utils/filter.js"
      via: "import { filterBusinesses }"
      pattern: "import.*filterBusinesses.*from.*utils/filter"
    - from: "tests/unit/phone.test.js"
      to: "src/utils/phone.js"
      via: "import { normalizePhone }"
      pattern: "import.*normalizePhone.*from.*utils/phone"
---

<objective>
Create the business website filter and Brazilian phone normalizer as pure functions with comprehensive unit tests following RED-then-GREEN TDD pattern.

Purpose: These two utilities are prerequisites for the outreach pipeline -- the filter ensures only businesses without real websites are contacted (SRCH-02), and the phone normalizer converts raw Brazilian numbers to E.164 format for Evolution API (WA-03). Both must be verified with tests before any send infrastructure exists.

Output: 4 files -- 2 test files (RED first, then GREEN), 2 source modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-filter-phone-normalization/02-CONTEXT.md
@.planning/phases/02-business-filter-phone-normalization/02-RESEARCH.md

<interfaces>
<!-- Prospect object shape from src/stages/fetch.js (line 16-22) -->
<!-- filterBusinesses reads the `website` field from this object -->

From src/stages/fetch.js:
```javascript
// Prospect object shape:
{
  placeId: place.id,
  name: place.displayName?.text ?? null,
  rating: place.rating ?? null,
  phone: place.nationalPhoneNumber ?? null,
  website: place.websiteUri ?? null,
  email: null
}
```

From tests/unit/fetch.test.js (established test pattern):
```javascript
import { describe, it, beforeEach, afterEach } from 'node:test'
import assert from 'node:assert/strict'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED unit tests for filter and phone modules</name>
  <files>tests/unit/filter.test.js, tests/unit/phone.test.js</files>
  <read_first>
    tests/unit/fetch.test.js
    .planning/phases/02-business-filter-phone-normalization/02-CONTEXT.md
    .planning/phases/02-business-filter-phone-normalization/02-RESEARCH.md
  </read_first>
  <behavior>
    filter.test.js (8 cases per SRCH-02):
    - filterBusinesses([{website: null}]) => length 1 (null = no site, include)
    - filterBusinesses([{website: ''}]) => length 1 (empty string = no site, include)
    - filterBusinesses([{website: '   '}]) => length 1 (whitespace = no site, include)
    - filterBusinesses([{website: 'instagram.com/business'}]) => length 1 (no protocol Instagram, include per D-03)
    - filterBusinesses([{website: 'https://www.instagram.com/business'}]) => length 1 (https Instagram with subdomain, include per D-02)
    - filterBusinesses([{website: 'https://linktr.ee/business'}]) => length 1 (linktr.ee, include per D-01)
    - filterBusinesses([{website: 'https://minhapadaria.com.br'}]) => length 0 (real website, exclude)
    - filterBusinesses([{website: '://invalid'}]) => length 1 (parse failure = no site, include per D-05)

    phone.test.js (12+ cases per WA-03 / D-14):
    - normalizePhone('+55 (11) 98765-4321') => '5511987654321' (full format with +55)
    - normalizePhone('+5511987654321') => '5511987654321' (compact with +55)
    - normalizePhone('(11) 98765-4321') => '5511987654321' (parens + dash, no country code)
    - normalizePhone('11987654321') => '5511987654321' (bare digits, 11 digits)
    - normalizePhone('5511987654321') => '5511987654321' (already E.164, 13 digits)
    - normalizePhone('11 98765-4321') => '5511987654321' (space-separated)
    - normalizePhone('(11)987654321') => '5511987654321' (parens no space)
    - normalizePhone('(11) 3456-7890') => '551134567890' (landline 8 digits, D-11)
    - normalizePhone('11 3456-7890') => '551134567890' (landline no parens)
    - normalizePhone('551134567890') => '551134567890' (landline already with 55, 12 digits)
    - normalizePhone(null) => null (no crash, D-07)
    - normalizePhone('12345') => null (unrecognized format, D-13, logs console.warn)
    - normalizePhone('') => null (empty string)
  </behavior>
  <action>
    Create `tests/unit/filter.test.js`:
    - Use `import { describe, it } from 'node:test'` and `import assert from 'node:assert/strict'` (same pattern as fetch.test.js)
    - Import `import { filterBusinesses } from '../../src/utils/filter.js'`
    - One `describe('filterBusinesses', () => { ... })` block with 8 `it()` test cases as listed in behavior
    - Each test creates a single-element array with the given `website` value, calls `filterBusinesses()`, asserts `result.length`
    - For the "real website excluded" case: `assert.equal(result.length, 0)`
    - For all "include" cases: `assert.equal(result.length, 1)`

    Create `tests/unit/phone.test.js`:
    - Same import pattern as above
    - Import `import { normalizePhone } from '../../src/utils/phone.js'`
    - One `describe('normalizePhone', () => { ... })` block with 13 `it()` test cases as listed in behavior
    - Each test calls `normalizePhone(input)` and asserts `assert.equal(result, expectedOutput)`
    - For the null/empty/unrecognized cases: `assert.equal(result, null)`
    - For the console.warn case ('12345'): also verify warn was called by temporarily replacing `console.warn` with a spy, asserting it was called with a string containing `'12345'`, then restoring original

    After writing both files, run `node --test tests/unit/filter.test.js tests/unit/phone.test.js` -- tests MUST FAIL (RED) because source modules do not exist yet. Commit with message: `test(02-01): add failing tests for filter and phone modules`
  </action>
  <verify>
    <automated>node --test tests/unit/filter.test.js tests/unit/phone.test.js 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/filter.test.js contains `import { filterBusinesses } from '../../src/utils/filter.js'`
    - tests/unit/filter.test.js contains `describe('filterBusinesses'`
    - tests/unit/filter.test.js contains at least 8 `it(` calls
    - tests/unit/filter.test.js contains `website: null` test case
    - tests/unit/filter.test.js contains `instagram.com/business` test case
    - tests/unit/filter.test.js contains `linktr.ee` test case
    - tests/unit/filter.test.js contains `minhapadaria.com.br` test case
    - tests/unit/phone.test.js contains `import { normalizePhone } from '../../src/utils/phone.js'`
    - tests/unit/phone.test.js contains `describe('normalizePhone'`
    - tests/unit/phone.test.js contains at least 12 `it(` calls
    - tests/unit/phone.test.js contains `5511987654321` expected output
    - tests/unit/phone.test.js contains `551134567890` expected output (landline)
    - tests/unit/phone.test.js contains test for `null` input
    - Test command exits with non-zero (RED -- source files do not exist yet)
  </acceptance_criteria>
  <done>Both test files exist with all specified test cases. Running tests produces RED failures because src/utils/filter.js and src/utils/phone.js do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement filter.js and phone.js to make tests GREEN</name>
  <files>src/utils/filter.js, src/utils/phone.js</files>
  <read_first>
    tests/unit/filter.test.js
    tests/unit/phone.test.js
    src/utils/env.js
    .planning/phases/02-business-filter-phone-normalization/02-RESEARCH.md
  </read_first>
  <behavior>
    filter.js:
    - filterBusinesses(prospects) returns prospects.filter(p => !hasRealWebsite(p.website))
    - hasRealWebsite(null) => false
    - hasRealWebsite('') => false
    - hasRealWebsite('   ') => false
    - hasRealWebsite('instagram.com/x') => false (prefixes https://, hostname is instagram.com)
    - hasRealWebsite('https://www.instagram.com/x') => false (hostname ends with .instagram.com)
    - hasRealWebsite('https://linktr.ee/x') => false (hostname is linktr.ee)
    - hasRealWebsite('https://minhapadaria.com.br') => true (not blocked)
    - hasRealWebsite('://invalid') => false (URL parse fails, caught by try/catch)

    phone.js:
    - Strip all non-digits: raw.replace(/\D/g, '')
    - 13 digits starting with 55 => return as-is (55 + DDD + 9-digit mobile)
    - 12 digits starting with 55 => return as-is (55 + DDD + 8-digit landline)
    - 11 digits => prepend '55' (DDD + 9-digit mobile)
    - 10 digits => prepend '55' (DDD + 8-digit landline)
    - Anything else => console.warn with raw value, return null
    - null/falsy input => return null immediately
  </behavior>
  <action>
    Create `src/utils/filter.js`:
    ```javascript
    const BLOCKED_DOMAINS = ['instagram.com', 'linktr.ee']

    function hasRealWebsite(website) {
      if (!website || !website.trim()) return false

      let href = website.trim()
      if (!href.startsWith('http://') && !href.startsWith('https://')) {
        href = 'https://' + href
      }

      let hostname
      try {
        hostname = new URL(href).hostname
      } catch {
        return false
      }

      const isBlocked = BLOCKED_DOMAINS.some(d => hostname === d || hostname.endsWith('.' + d))
      return !isBlocked
    }

    export function filterBusinesses(prospects) {
      return prospects.filter(prospect => !hasRealWebsite(prospect.website))
    }
    ```

    Create `src/utils/phone.js`:
    ```javascript
    export function normalizePhone(raw) {
      if (!raw) return null

      const digits = raw.replace(/\D/g, '')

      if ((digits.length === 13 || digits.length === 12) && digits.startsWith('55')) {
        return digits
      }

      if (digits.length === 11 || digits.length === 10) {
        return '55' + digits
      }

      console.warn(`[phone] normalizePhone: unrecognized format, skipping — raw="${raw}"`)
      return null
    }
    ```

    After creating both files, run `node --test tests/unit/filter.test.js tests/unit/phone.test.js` -- ALL tests MUST PASS (GREEN).
    Then run `npm test` to confirm full suite (including existing fetch tests) still passes.
    Commit with message: `feat(02-01): implement filterBusinesses and normalizePhone`
  </action>
  <verify>
    <automated>node --test tests/unit/filter.test.js tests/unit/phone.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/filter.js contains `export function filterBusinesses(`
    - src/utils/filter.js contains `const BLOCKED_DOMAINS = ['instagram.com', 'linktr.ee']`
    - src/utils/filter.js contains `new URL(href).hostname`
    - src/utils/filter.js contains `hostname.endsWith('.' + d)`
    - src/utils/filter.js contains `prospects.filter(`
    - src/utils/phone.js contains `export function normalizePhone(`
    - src/utils/phone.js contains `raw.replace(/\D/g, '')`
    - src/utils/phone.js contains `digits.startsWith('55')`
    - src/utils/phone.js contains `'55' + digits`
    - src/utils/phone.js contains `console.warn(`
    - `node --test tests/unit/filter.test.js tests/unit/phone.test.js` exits 0 (all GREEN)
    - `npm test` exits 0 (full suite GREEN including fetch.test.js)
  </acceptance_criteria>
  <done>All filter and phone tests pass GREEN. Full test suite (`npm test`) passes. Both modules export the correct functions per D-06 and D-07.</done>
</task>

</tasks>

<verification>
1. `node --test tests/unit/filter.test.js` -- all 8 filter cases pass
2. `node --test tests/unit/phone.test.js` -- all 12+ phone cases pass
3. `npm test` -- full suite passes (filter + phone + existing fetch tests)
4. `grep -c "export function filterBusinesses" src/utils/filter.js` returns 1
5. `grep -c "export function normalizePhone" src/utils/phone.js` returns 1
</verification>

<success_criteria>
- filterBusinesses correctly includes businesses with null/empty/Instagram/linktr.ee websites and excludes those with real websites
- normalizePhone correctly converts all 10 D-14 Brazilian phone variants to E.164 format
- normalizePhone returns null for unrecognized formats and logs a warning
- All tests pass GREEN via `npm test`
- No external dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-filter-phone-normalization/02-01-SUMMARY.md`
</output>
