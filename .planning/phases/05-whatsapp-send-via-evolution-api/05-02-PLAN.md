---
phase: 05-whatsapp-send-via-evolution-api
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - bin/prospect.js
autonomous: false
requirements:
  - WA-01
  - WA-02

must_haves:
  truths:
    - "Pipeline executa na ordem: validateEnv -> healthCheck -> fetchProspects -> dedupProspects -> loop(renderMessage + sendWhatsApp)"
    - "Instancia desconectada imprime erro e faz process.exit(1) antes de buscar prospects"
    - "Cada contato produz exatamente uma linha de status: [WA sent] ou [WA failed: motivo]"
    - "Falha em um contato nao impede processamento dos demais"
    - "Exit code 0 mesmo se todos os contatos falharem"
  artifacts:
    - path: "bin/prospect.js"
      provides: "Pipeline completo v1: env -> health -> fetch -> dedup -> render -> send WA"
      min_lines: 40
  key_links:
    - from: "bin/prospect.js"
      to: "src/services/evolution.js"
      via: "import { checkConnection }"
      pattern: "import.*checkConnection.*evolution"
    - from: "bin/prospect.js"
      to: "src/stages/sender.js"
      via: "import { sendWhatsApp }"
      pattern: "import.*sendWhatsApp.*sender"
    - from: "bin/prospect.js"
      to: "src/stages/render.js"
      via: "import { renderMessage }"
      pattern: "import.*renderMessage.*render"
    - from: "bin/prospect.js"
      to: "src/stages/dedup.js"
      via: "import { dedupProspects }"
      pattern: "import.*dedupProspects.*dedup"
    - from: "bin/prospect.js"
      to: "src/history.js"
      via: "import { loadHistory }"
      pattern: "import.*loadHistory.*history"
---

<objective>
Transformar bin/prospect.js de script de debug (console.log JSON) em orquestrador completo do pipeline v1: health check, fetch, dedup, render, send WhatsApp.

Purpose: Sem este wiring, os modulos criados no Plan 01 nao sao executaveis. Este plan faz o bot funcionar de ponta a ponta.
Output: bin/prospect.js com pipeline completo, pronto para teste real.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-whatsapp-send-via-evolution-api/05-CONTEXT.md
@.planning/phases/05-whatsapp-send-via-evolution-api/05-RESEARCH.md
@.planning/phases/05-whatsapp-send-via-evolution-api/05-01-SUMMARY.md

<interfaces>
<!-- Modules available after Plan 01 -->

From src/services/evolution.js:
```javascript
export async function checkConnection({ baseUrl, apiKey, instance })
// GET /instance/connectionState/{instance}
// Returns: { instance: { instanceName, state: 'open' | 'close' | 'connecting' } }
// Throws on non-ok response. Has 5s AbortController timeout.

export async function sendTextMessage({ baseUrl, apiKey, instance, number, text })
// POST /message/sendText/{instance}
// Returns: { key, message, messageTimestamp, status: 'PENDING' }
// Throws on non-ok response.
```

From src/stages/sender.js:
```javascript
export async function sendWhatsApp(prospect, message, config)
// config = { baseUrl, apiKey, instance }
// Returns: { ok: true } on success (calls recordSend internally)
// Returns: { ok: false, reason: string } on failure (no recordSend)
// Applies random 3-8s delay after each send internally
```

From src/utils/env.js (expanded):
```javascript
export function validateEnv()
// Returns: { apiKey, evolutionApiUrl, evolutionApiKey, evolutionInstance }
// Throws Error('VARNAME is missing or empty in .env') if any is missing
```

From src/stages/fetch.js:
```javascript
export async function fetchProspects({ city, category, apiKey })
// Returns array of prospect objects with { placeId, name, phone, rating, ... }
```

From src/stages/dedup.js:
```javascript
export function dedupProspects(prospects)
// Returns filtered array excluding already-contacted prospects
```

From src/stages/render.js:
```javascript
export function renderMessage(prospect, { cidade, categoria })
// Returns rendered message string
```

From src/history.js:
```javascript
export function loadHistory()
// Loads history.json into memory Map. Must be called once at startup.
```

From src/utils/args.js:
```javascript
export function parseArgs(argv)
// Returns { city, category }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever bin/prospect.js com pipeline completo v1</name>
  <files>bin/prospect.js</files>
  <read_first>
    - bin/prospect.js (estado atual — script de debug com console.log JSON)
    - src/services/evolution.js (checkConnection — assinatura e retorno)
    - src/stages/sender.js (sendWhatsApp — assinatura e contrato { ok, reason })
    - src/utils/env.js (validateEnv — agora retorna objeto)
    - src/stages/fetch.js (fetchProspects — assinatura)
    - src/stages/dedup.js (dedupProspects — assinatura)
    - src/stages/render.js (renderMessage — assinatura)
    - src/history.js (loadHistory — deve ser chamado uma vez no startup)
  </read_first>
  <action>
    Substituir o conteudo completo de bin/prospect.js pelo pipeline v1:

    ```javascript
    import 'dotenv/config'
    import { parseArgs } from '../src/utils/args.js'
    import { validateEnv } from '../src/utils/env.js'
    import { checkConnection } from '../src/services/evolution.js'
    import { fetchProspects } from '../src/stages/fetch.js'
    import { loadHistory } from '../src/history.js'
    import { dedupProspects } from '../src/stages/dedup.js'
    import { renderMessage } from '../src/stages/render.js'
    import { sendWhatsApp } from '../src/stages/sender.js'

    // 1. Validate environment (all 4 vars per D-14)
    let env
    try {
      env = validateEnv()
    } catch (err) {
      console.error(`Error: ${err.message}`)
      process.exit(1)
    }

    // 2. Parse CLI args
    const { city, category } = parseArgs(process.argv)
    if (!city || !category) {
      console.error('Usage: node bin/prospect.js --city "Sao Paulo" --category "restaurante"')
      process.exit(1)
    }

    // 3. Health check Evolution API instance (per D-05, D-06, D-07)
    try {
      const status = await checkConnection({
        baseUrl: env.evolutionApiUrl,
        apiKey: env.evolutionApiKey,
        instance: env.evolutionInstance
      })
      if (status.instance?.state !== 'open') {
        console.error('Error: Evolution API instance not connected. Reconnect via QR code before running.')
        process.exit(1)
      }
    } catch (err) {
      console.error(`Error: Evolution API instance not connected. Reconnect via QR code before running.`)
      process.exit(1)
    }

    // 4. Fetch prospects
    let prospects
    try {
      prospects = await fetchProspects({ city, category, apiKey: env.apiKey })
    } catch (err) {
      console.error(`Error fetching prospects: ${err.message}`)
      process.exit(1)
    }

    // 5. Load history and dedup
    loadHistory()
    prospects = dedupProspects(prospects)

    if (prospects.length === 0) {
      console.log('No new prospects to contact.')
      process.exit(0)
    }

    // 6. Render + Send WhatsApp for each prospect (per D-03, D-04, D-09)
    const waConfig = {
      baseUrl: env.evolutionApiUrl,
      apiKey: env.evolutionApiKey,
      instance: env.evolutionInstance
    }

    for (const prospect of prospects) {
      const message = renderMessage(prospect, { cidade: city, categoria: category })
      const result = await sendWhatsApp(prospect, message, waConfig)
      if (result.ok) {
        console.log(`[WA sent] ${prospect.name}`)
      } else {
        console.log(`[WA failed: ${result.reason}] ${prospect.name}`)
      }
    }

    process.exit(0)
    ```

    Pontos criticos implementados:
    - Per D-07: health check ANTES de fetchProspects
    - Per D-05: verifica `status.instance?.state !== 'open'` (campo `state`, NAO `connectionStatus` — ver Pitfall 1 do RESEARCH.md)
    - Per D-06: mensagem exata "Error: Evolution API instance not connected. Reconnect via QR code before running."
    - Per D-03: output `[WA sent] NomeEmpresa` e `[WA failed: motivo] NomeEmpresa`
    - Per D-04: process.exit(0) sempre — falhas por contato sao esperadas
    - Per D-09: entry point unico com pipeline completo
    - Per Pitfall 2: `env = validateEnv()` retorna objeto, desestruturado em `env.apiKey`, `env.evolutionApiUrl`, etc.
    - loadHistory() chamado ANTES de dedupProspects (HIST requirement)
  </action>
  <verify>
    <automated>node -c bin/prospect.js && echo "syntax ok"</automated>
    Verificar que a sintaxe esta correta. Nao e possivel rodar o pipeline completo sem env vars reais, mas a sintaxe deve ser valida.
  </verify>
  <acceptance_criteria>
    - bin/prospect.js contem `import { checkConnection } from '../src/services/evolution.js'`
    - bin/prospect.js contem `import { sendWhatsApp } from '../src/stages/sender.js'`
    - bin/prospect.js contem `import { loadHistory } from '../src/history.js'`
    - bin/prospect.js contem `import { dedupProspects } from '../src/stages/dedup.js'`
    - bin/prospect.js contem `import { renderMessage } from '../src/stages/render.js'`
    - bin/prospect.js contem `env = validateEnv()` (nao `apiKey = validateEnv()`)
    - bin/prospect.js contem `checkConnection(` chamado ANTES de `fetchProspects(`
    - bin/prospect.js contem `status.instance?.state !== 'open'`  (campo state, NAO connectionStatus)
    - bin/prospect.js contem `'Error: Evolution API instance not connected. Reconnect via QR code before running.'`
    - bin/prospect.js contem `loadHistory()` chamado ANTES de `dedupProspects(`
    - bin/prospect.js contem `[WA sent]`
    - bin/prospect.js contem `[WA failed:`
    - bin/prospect.js contem `process.exit(0)` como ultima linha significativa
    - bin/prospect.js NAO contem `console.log(JSON.stringify(results` (debug removido)
    - node -c bin/prospect.js exits 0 (sintaxe valida)
    - npm test exits 0 (nenhum teste quebrado)
  </acceptance_criteria>
  <done>bin/prospect.js transformado em orquestrador completo do pipeline v1. Pipeline: validateEnv -> healthCheck -> fetchProspects -> loadHistory -> dedupProspects -> loop(renderMessage + sendWhatsApp). Output por contato com [WA sent] ou [WA failed]. Exit 0 sempre.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar pipeline completo com instancia Evolution API real</name>
  <files>bin/prospect.js</files>
  <action>
    Este e um checkpoint de verificacao humana. Nenhuma automacao — o usuario testa o pipeline completo com uma instancia Evolution API real conectada.
  </action>
  <what-built>Pipeline completo de outreach WhatsApp: bin/prospect.js faz health check, busca prospects, dedup, renderiza template, e envia WhatsApp via Evolution API com delay 3-8s entre envios.</what-built>
  <how-to-verify>
    1. Garantir que .env contem as 4 vars: GOOGLE_PLACES_API_KEY, EVOLUTION_API_URL, EVOLUTION_API_KEY, EVOLUTION_INSTANCE
    2. Garantir que a instancia Evolution API esta conectada (QR code escaneado)
    3. Rodar: `node bin/prospect.js --city "Campinas" --category "padaria"`
    4. Verificar no terminal:
       - Health check passa (nao imprime erro de instancia)
       - Prospects sao buscados e filtrados
       - Cada contato produz uma linha `[WA sent] NomeDaEmpresa` ou `[WA failed: motivo] NomeDaEmpresa`
       - Delay visivelmente diferente entre cada envio (3-8 segundos)
       - Processo termina com exit code 0
    5. (Opcional) Verificar que os numeros reais receberam a mensagem no WhatsApp
    6. Rodar o MESMO comando novamente — todos os contatos devem ser pulados (dedup funcional, output "No new prospects to contact.")
  </how-to-verify>
  <verify>
    <automated>node -c bin/prospect.js && npm test</automated>
  </verify>
  <done>Usuario confirmou que pipeline funciona de ponta a ponta com instancia Evolution API real.</done>
  <resume-signal>Digitar "approved" se o pipeline funciona, ou descrever problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `node -c bin/prospect.js` — sintaxe valida
- `npm test` — suite completa verde (nenhum teste quebrado)
- Pipeline order: validateEnv -> healthCheck -> fetch -> loadHistory -> dedup -> render+send loop
- Cada contato com output [WA sent] ou [WA failed]
- Exit code 0 em todos os cenarios
</verification>

<success_criteria>
- WA-01: Bot envia WhatsApp via Evolution API, trata falhas por contato, verifica health no startup
- WA-02: Delay 3-8s entre envios visivel no terminal
- Pipeline completo roda de ponta a ponta com um comando
- Dedup funcional na segunda execucao
</success_criteria>

<output>
After completion, create `.planning/phases/05-whatsapp-send-via-evolution-api/05-02-SUMMARY.md`
</output>
