---
phase: 05-whatsapp-send-via-evolution-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/evolution.test.js
  - tests/unit/sender.test.js
  - tests/unit/env.test.js
  - src/services/evolution.js
  - src/stages/sender.js
  - src/utils/env.js
  - .env.example
autonomous: true
requirements:
  - WA-01
  - WA-02

must_haves:
  truths:
    - "checkConnection faz GET para /instance/connectionState/{instance} com header apikey"
    - "checkConnection aplica timeout de 5s via AbortController e limpa o timer com clearTimeout"
    - "sendTextMessage faz POST para /message/sendText/{instance} com body { number, text }"
    - "sendWhatsApp retorna { ok: true } e chama recordSend em sucesso"
    - "sendWhatsApp retorna { ok: false, reason } sem lancar excecao em falha"
    - "sendWhatsApp NAO chama recordSend em falha"
    - "Delay entre envios e >= 3000ms e <= 8000ms"
    - "validateEnv valida 4 vars e retorna objeto com apiKey, evolutionApiUrl, evolutionApiKey, evolutionInstance"
  artifacts:
    - path: "src/services/evolution.js"
      provides: "HTTP client Evolution API (checkConnection + sendTextMessage)"
      exports: ["checkConnection", "sendTextMessage"]
    - path: "src/stages/sender.js"
      provides: "Pipeline stage de envio WA com delay e recordSend"
      exports: ["sendWhatsApp"]
    - path: "src/utils/env.js"
      provides: "Validacao expandida de 4 env vars"
      exports: ["validateEnv"]
    - path: "tests/unit/evolution.test.js"
      provides: "Testes unitarios para checkConnection e sendTextMessage"
    - path: "tests/unit/sender.test.js"
      provides: "Testes unitarios para sendWhatsApp e delay"
    - path: "tests/unit/env.test.js"
      provides: "Testes expandidos para 4 env vars"
  key_links:
    - from: "src/stages/sender.js"
      to: "src/services/evolution.js"
      via: "import { sendTextMessage }"
      pattern: "import.*sendTextMessage.*evolution"
    - from: "src/stages/sender.js"
      to: "src/history.js"
      via: "import { recordSend }"
      pattern: "import.*recordSend.*history"
---

<objective>
Criar os modulos de servico e stage para envio WhatsApp via Evolution API, com testes TDD (RED primeiro, GREEN depois).

Purpose: WA-01 e WA-02 dependem de `evolution.js` (HTTP client) e `sender.js` (stage com delay + recordSend). Tambem expandir `validateEnv` para as 3 novas env vars da Evolution API.
Output: `src/services/evolution.js`, `src/stages/sender.js`, `src/utils/env.js` expandido, `.env.example` atualizado, todos os testes verdes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-whatsapp-send-via-evolution-api/05-CONTEXT.md
@.planning/phases/05-whatsapp-send-via-evolution-api/05-RESEARCH.md

<interfaces>
<!-- Existing modules consumed by this plan -->

From src/services/places.js (pattern to replicate in evolution.js):
```javascript
export async function searchPlaces({ query, pageToken = null, apiKey }) {
  // stateless HTTP function — receives params, returns JSON
  const response = await fetch(url, { method: 'POST', headers, body })
  if (!response.ok) { throw new Error(`Places API error ${response.status}: ${err}`) }
  return response.json()
}
```

From src/history.js (recordSend to call from sender.js):
```javascript
export function recordSend(placeId) {
  // writes to history.json atomically (write-then-rename)
}
```

From src/utils/env.js (current — will be replaced):
```javascript
export function validateEnv() {
  const key = process.env.GOOGLE_PLACES_API_KEY
  if (!key) { throw new Error('GOOGLE_PLACES_API_KEY is missing or empty in .env') }
  return key  // returns string — will change to return object
}
```

From tests/unit/env.test.js (current — will be expanded):
```javascript
// Tests: returns key when set, throws when missing, throws when empty
// Uses beforeEach/afterEach to save/restore process.env.GOOGLE_PLACES_API_KEY
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Criar testes para evolution.js, sender.js, e expandir env.test.js</name>
  <files>tests/unit/evolution.test.js, tests/unit/sender.test.js, tests/unit/env.test.js</files>
  <read_first>
    - tests/unit/places.test.js (padrao de mock de globalThis.fetch a replicar)
    - tests/unit/env.test.js (testes existentes a expandir)
    - src/services/places.js (padrao de modulo a replicar)
    - src/history.js (recordSend que sender.js vai chamar)
  </read_first>
  <behavior>
    evolution.test.js — checkConnection:
    - Test 1: checkConnection faz fetch para `${baseUrl}/instance/connectionState/${instance}` com header `apikey: ${apiKey}`
    - Test 2: checkConnection retorna JSON parseado quando response.ok === true
    - Test 3: checkConnection lanca Error com status code quando response.ok === false
    - Test 4: checkConnection passa signal de AbortController ao fetch (verificar que opts.signal existe)
    - Test 5: checkConnection limpa timeout via clearTimeout apos sucesso (nao deixa timer pendente)

    evolution.test.js — sendTextMessage:
    - Test 6: sendTextMessage faz POST para `${baseUrl}/message/sendText/${instance}` com headers `Content-Type: application/json` e `apikey: ${apiKey}`, body `{ number, text }`
    - Test 7: sendTextMessage retorna JSON parseado em sucesso
    - Test 8: sendTextMessage lanca Error com status code quando response.ok === false

    sender.test.js — sendWhatsApp:
    - Test 9: sendWhatsApp retorna `{ ok: true }` quando sendTextMessage resolve
    - Test 10: sendWhatsApp chama recordSend(prospect.placeId) quando sendTextMessage resolve
    - Test 11: sendWhatsApp retorna `{ ok: false, reason: 'mensagem de erro' }` quando sendTextMessage rejeita
    - Test 12: sendWhatsApp NAO chama recordSend quando sendTextMessage rejeita
    - Test 13: sendWhatsApp aplica delay >= 3000ms e <= 8000ms apos envio (mock setTimeout para verificar argumento)

    env.test.js — expandido:
    - Test 14: validateEnv retorna objeto { apiKey, evolutionApiUrl, evolutionApiKey, evolutionInstance } quando todas as 4 vars estao setadas
    - Test 15: validateEnv lanca Error mencionando 'EVOLUTION_API_URL' quando essa var esta ausente
    - Test 16: validateEnv lanca Error mencionando 'EVOLUTION_API_KEY' quando essa var esta ausente
    - Test 17: validateEnv lanca Error mencionando 'EVOLUTION_INSTANCE' quando essa var esta ausente
    - Test 18: validateEnv continua lancando Error mencionando 'GOOGLE_PLACES_API_KEY' quando essa var esta ausente
  </behavior>
  <action>
    Criar `tests/unit/evolution.test.js`:
    - import { describe, it, beforeEach, afterEach } from 'node:test'
    - import assert from 'node:assert/strict'
    - import { checkConnection, sendTextMessage } from '../../src/services/evolution.js'
    - Mock globalThis.fetch no beforeEach, restaurar no afterEach (mesmo padrao de places.test.js)
    - Para Test 4 (signal): mock fetch que captura opts.signal e verifica que e uma instancia de AbortSignal
    - Para Test 5 (clearTimeout): usar mock de setTimeout que registra chamadas, verificar que clearTimeout foi chamado
    - Params de teste: baseUrl='http://localhost:8080', apiKey='test-key', instance='test-instance'

    Criar `tests/unit/sender.test.js`:
    - import { describe, it, beforeEach, afterEach } from 'node:test'
    - import assert from 'node:assert/strict'
    - Mock de `sendTextMessage` e `recordSend` via substituicao dinamica de modulo OU:
      - Abordagem preferida: sender.js recebe dependencies via parametro de config ou importa diretamente.
      - Para testar sem dependencia real: mock globalThis.fetch (sendTextMessage usa fetch) e mock de history.js.
      - Alternativa simples: sender.js importa sendTextMessage e recordSend diretamente. No teste, mock fetch para controlar sendTextMessage, e usar mock de fs para controlar recordSend.
      - Decisao: usar a abordagem mais simples que funcione com node:test — mock globalThis.fetch e verificar efeitos colaterais.
    - Para Test 13 (delay): mock global setTimeout para capturar o argumento de delay. Verificar que delay >= 3000 e <= 8000.
    - Prospect de teste: { placeId: 'place123', name: 'Test Biz', phone: '5511999998888' }

    Expandir `tests/unit/env.test.js`:
    - MANTER todos os testes existentes (3 testes para GOOGLE_PLACES_API_KEY)
    - ADICIONAR beforeEach/afterEach para salvar/restaurar as 3 novas vars: EVOLUTION_API_URL, EVOLUTION_API_KEY, EVOLUTION_INSTANCE
    - ADICIONAR describe('validateEnv — Evolution API vars') com Tests 14-17
    - Test 14: setar todas as 4 vars, assert.deepStrictEqual no retorno do objeto
    - Tests 15-17: deletar uma var por vez, assert.throws com mensagem contendo o nome da var

    IMPORTANTE: Os testes devem FALHAR (RED) neste momento porque os modulos source ainda nao existem ou nao exportam as funcoes esperadas. Criar arquivos stub vazios se necessario para que o import nao cause erro de modulo nao encontrado, mas as funcoes devem falhar.

    Stubs minimos para permitir import sem crash:
    - src/services/evolution.js: `export async function checkConnection() {} export async function sendTextMessage() {}`
    - src/stages/sender.js: `export async function sendWhatsApp() {}`
  </action>
  <verify>
    <automated>node --test tests/unit/evolution.test.js tests/unit/sender.test.js tests/unit/env.test.js 2>&1 | tail -5</automated>
    Os testes devem rodar (nao crashar no import) mas a maioria deve FALHAR (RED). Confirmar que pelo menos 10 testes falham.
  </verify>
  <acceptance_criteria>
    - tests/unit/evolution.test.js contem `import { checkConnection, sendTextMessage }` na primeira linha de import
    - tests/unit/evolution.test.js contem `describe('checkConnection'`
    - tests/unit/evolution.test.js contem `describe('sendTextMessage'`
    - tests/unit/evolution.test.js contem pelo menos 8 chamadas `it(`
    - tests/unit/sender.test.js contem `import { sendWhatsApp }` ou equivalente
    - tests/unit/sender.test.js contem pelo menos 5 chamadas `it(`
    - tests/unit/sender.test.js contem `recordSend` em pelo menos 2 assertions
    - tests/unit/sender.test.js contem `3000` e `8000` (delay bounds)
    - tests/unit/env.test.js contem `EVOLUTION_API_URL` em pelo menos 1 test
    - tests/unit/env.test.js contem `EVOLUTION_API_KEY` em pelo menos 1 test
    - tests/unit/env.test.js contem `EVOLUTION_INSTANCE` em pelo menos 1 test
    - tests/unit/env.test.js contem `apiKey` e `evolutionApiUrl` (propriedades do objeto retornado)
    - src/services/evolution.js existe (stub com exports)
    - src/stages/sender.js existe (stub com export)
    - node --test tests/unit/evolution.test.js 2>&1 contem "fail" (RED)
  </acceptance_criteria>
  <done>Todos os arquivos de teste criados com cobertura completa dos comportamentos de WA-01 e WA-02. Stubs criados para evitar crash de import. Testes rodam mas falham (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar evolution.js, sender.js, expandir env.js, atualizar .env.example</name>
  <files>src/services/evolution.js, src/stages/sender.js, src/utils/env.js, .env.example</files>
  <read_first>
    - tests/unit/evolution.test.js (testes que devem passar)
    - tests/unit/sender.test.js (testes que devem passar)
    - tests/unit/env.test.js (testes que devem passar)
    - src/services/places.js (padrao de modulo HTTP a replicar)
    - src/history.js (recordSend — assinatura e comportamento)
    - src/utils/env.js (codigo atual a expandir)
    - .env.example (arquivo atual a expandir)
  </read_first>
  <behavior>
    - checkConnection({ baseUrl, apiKey, instance }) faz GET com AbortController timeout 5s, retorna JSON
    - sendTextMessage({ baseUrl, apiKey, instance, number, text }) faz POST, retorna JSON
    - sendWhatsApp(prospect, message) chama sendTextMessage, recordSend em sucesso, delay 3-8s, retorna { ok, reason? }
    - validateEnv() retorna { apiKey, evolutionApiUrl, evolutionApiKey, evolutionInstance }
  </behavior>
  <action>
    **src/services/evolution.js** — substituir o stub:
    ```javascript
    export async function checkConnection({ baseUrl, apiKey, instance }) {
      const controller = new AbortController()
      const timeoutId = setTimeout(() => controller.abort(), 5000)
      try {
        const response = await fetch(
          `${baseUrl}/instance/connectionState/${instance}`,
          { headers: { apikey: apiKey }, signal: controller.signal }
        )
        if (!response.ok) {
          const err = await response.text()
          throw new Error(`Evolution API error ${response.status}: ${err}`)
        }
        return response.json()
      } finally {
        clearTimeout(timeoutId)
      }
    }

    export async function sendTextMessage({ baseUrl, apiKey, instance, number, text }) {
      const response = await fetch(`${baseUrl}/message/sendText/${instance}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', apikey: apiKey },
        body: JSON.stringify({ number, text })
      })
      if (!response.ok) {
        const err = await response.text()
        throw new Error(`Evolution API error ${response.status}: ${err}`)
      }
      return response.json()
    }
    ```

    **src/stages/sender.js** — substituir o stub:
    ```javascript
    import { sendTextMessage } from '../services/evolution.js'
    import { recordSend } from '../history.js'

    export async function sendWhatsApp(prospect, message, config) {
      // config = { baseUrl, apiKey, instance }
      try {
        await sendTextMessage({
          baseUrl: config.baseUrl,
          apiKey: config.apiKey,
          instance: config.instance,
          number: prospect.phone,
          text: message
        })
        recordSend(prospect.placeId)
        // Delay 3-8 seconds after send (per D-12, WA-02)
        const delay = 3000 + Math.floor(Math.random() * 5001)
        await new Promise(resolve => setTimeout(resolve, delay))
        return { ok: true }
      } catch (err) {
        return { ok: false, reason: err.message }
      }
    }
    ```
    NOTA: `config` passado como 3o parametro para que sender.js nao dependa de process.env diretamente (testabilidade). O bin/prospect.js passara o config extraido de validateEnv().

    **src/utils/env.js** — substituir conteudo completo:
    ```javascript
    export function validateEnv() {
      const required = {
        apiKey:            process.env.GOOGLE_PLACES_API_KEY,
        evolutionApiUrl:   process.env.EVOLUTION_API_URL,
        evolutionApiKey:   process.env.EVOLUTION_API_KEY,
        evolutionInstance: process.env.EVOLUTION_INSTANCE
      }
      const envNames = {
        apiKey:            'GOOGLE_PLACES_API_KEY',
        evolutionApiUrl:   'EVOLUTION_API_URL',
        evolutionApiKey:   'EVOLUTION_API_KEY',
        evolutionInstance: 'EVOLUTION_INSTANCE'
      }
      for (const [key, value] of Object.entries(required)) {
        if (!value) {
          throw new Error(`${envNames[key]} is missing or empty in .env`)
        }
      }
      return required
    }
    ```

    **.env.example** — substituir conteudo completo:
    ```
    # Google Places API key — required for all searches
    # Get yours at: https://console.cloud.google.com/apis/credentials
    # IMPORTANT: Before running, configure in Google Cloud Console:
    #   1. Enable "Places API (New)" in APIs & Services
    #   2. Set a $10 billing alert in Billing > Budgets & Alerts
    #   3. Set a daily quota cap in APIs & Services > Places API (New) > Quotas
    GOOGLE_PLACES_API_KEY=

    # Evolution API — required for WhatsApp sending
    # Your Evolution API server URL (e.g., http://localhost:8080)
    EVOLUTION_API_URL=
    # API key from Evolution API dashboard
    EVOLUTION_API_KEY=
    # Instance name configured in Evolution API
    EVOLUTION_INSTANCE=
    ```

    IMPORTANTE per D-14: validateEnv retorna OBJETO, nao string. O bin/prospect.js sera atualizado no Plan 02 para desestruturar o retorno. Os testes existentes de env.test.js precisam ser atualizados para refletir o novo contrato (retorna objeto em vez de string).
  </action>
  <verify>
    <automated>node --test tests/unit/evolution.test.js tests/unit/sender.test.js tests/unit/env.test.js</automated>
    Todos os testes devem passar (GREEN).
  </verify>
  <acceptance_criteria>
    - src/services/evolution.js contem `export async function checkConnection(`
    - src/services/evolution.js contem `export async function sendTextMessage(`
    - src/services/evolution.js contem `AbortController`
    - src/services/evolution.js contem `clearTimeout(timeoutId)`
    - src/services/evolution.js contem `headers: { apikey: apiKey }`  (nao Bearer)
    - src/services/evolution.js contem `/instance/connectionState/`
    - src/services/evolution.js contem `/message/sendText/`
    - src/stages/sender.js contem `export async function sendWhatsApp(`
    - src/stages/sender.js contem `import { sendTextMessage }` de evolution.js
    - src/stages/sender.js contem `import { recordSend }` de history.js
    - src/stages/sender.js contem `recordSend(prospect.placeId)` dentro do try (antes do catch)
    - src/stages/sender.js contem `3000 + Math.floor(Math.random() * 5001)`
    - src/stages/sender.js contem `return { ok: true }`
    - src/stages/sender.js contem `return { ok: false, reason:`
    - src/utils/env.js contem `EVOLUTION_API_URL`
    - src/utils/env.js contem `EVOLUTION_API_KEY`
    - src/utils/env.js contem `EVOLUTION_INSTANCE`
    - src/utils/env.js contem `return required` (retorna objeto)
    - .env.example contem `EVOLUTION_API_URL=`
    - .env.example contem `EVOLUTION_API_KEY=`
    - .env.example contem `EVOLUTION_INSTANCE=`
    - node --test tests/unit/evolution.test.js tests/unit/sender.test.js tests/unit/env.test.js exits 0
    - npm test exits 0 (suite completa verde)
  </acceptance_criteria>
  <done>Todos os modulos implementados, todos os testes verdes (GREEN). evolution.js exporta checkConnection e sendTextMessage. sender.js exporta sendWhatsApp com delay e recordSend. validateEnv retorna objeto com 4 vars. .env.example atualizado.</done>
</task>

</tasks>

<verification>
- `node --test tests/unit/evolution.test.js` — todos os testes de checkConnection e sendTextMessage passam
- `node --test tests/unit/sender.test.js` — todos os testes de sendWhatsApp e delay passam
- `node --test tests/unit/env.test.js` — testes existentes + novos passam (validateEnv retorna objeto)
- `npm test` — suite completa verde (nenhum teste quebrado de fases anteriores)
</verification>

<success_criteria>
- WA-01 coberto: checkConnection, sendTextMessage, sendWhatsApp com contrato { ok, reason }
- WA-02 coberto: delay 3-8s verificado em teste
- Nenhum teste de fases anteriores quebrado
- Modulos prontos para wiring no Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/05-whatsapp-send-via-evolution-api/05-01-SUMMARY.md`
</output>
